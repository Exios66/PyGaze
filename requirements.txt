# Core dependencies
pygaze==1.2.2
pygame==2.2.0; sys_platform == 'darwin'  # Required for macOS compatibility
numpy==1.26.4
Pillow==10.1.0
python-dateutil==2.8.2

# Windows-specific (alternative webcam)
opencv-python-headless>=4.8.0  # Recommended over VideoCapture

# Linux dependencies (install via apt first):
# sudo apt-get install python3-dev libsdl2-dev libsdl2-image-dev

# PsychoPy integration
psychopy>=2023.2.3

# Data analysis
pandas>=2.1.3
scipy>=1.11.3
matplotlib>=3.8.0

# Hardware integration
pyserial>=3.5

# Modern Python
typing-extensions>=4.8.0

# Platform-specific SDL paths (add at end)
--config-settings="--global-option=build_ext" \
--config-settings="--global-option=-I/opt/homebrew/opt/sdl2/include" \
--config-settings="--global-option=-L/opt/homebrew/opt/sdl2/lib" 